# install with conda env create -f environment.yml 
name: 'churn_project'
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - ipykernel
  - ipywidgets
  - matplotlib
  - plotly
  - scikit-learn
  - pip
  - pytorch
  - altair
  - seaborn
  - pip:
      - pandas
      - numpy
      - torch
      - openpyxl
